# RMK — RELATIONAL MESH KERNEL (ANCHOR)
Version: 1.0  
Status: FORMAL  
Layer: Φ Substrate  

---

## 0. ONTOLOGICAL POSITION

Relational Mesh Kernel (RMK) is NOT:

- an algorithm
- an engine
- a model
- a decision system

RMK is:

> a minimal relational substrate of Φ

It defines the conditions under which relational structure can exist.

---

## 1. FORMAL DEFINITION

Φ = (Σ, R)

where:

- Σ = set of invariant singularities  
- R ∈ so(n), antisymmetric relational matrix  

Constraints:

- R(i,j) = -R(j,i)  
- no privileged node  
- no absolute reference frame  

For n = 8:

- edges = 28  
- 3-cycles = 56  

---

## 2. INVARIANTS (NON-NEGOTIABLE)

RMK must satisfy:

1. Antisymmetry  
2. Permutation invariance (Sₙ)  
3. No central authority  
4. No objective function  
5. No feedback loop  

Violation of any:

→ RMK is invalid

---

## 3. NON-FUNCTIONAL NATURE

RMK does NOT:

- optimize  
- evaluate  
- decide  
- learn  
- store history  

RMK is:

> purely descriptive

---

## 4. RELATIONAL STRUCTURE

Each relation:

R(i,j) ∈ ℝ

Interpretation:

- sign → direction of tension  
- magnitude → intensity  

---

## 5. TRIADIC STRUCTURE (Δ)

For any (i,j,k):

Δ(i,j,k) = R(i,j) + R(j,k) + R(k,i)

Interpretation:

- Δ = 0 → local coherence  
- Δ ≠ 0 → deformation  

Δ is invariant under permutation of indices.

---

## 6. DERIVED METRICS (NON-RMK)

The following are NOT part of RMK,
but are computable from it:

- μ (epistemic uncertainty)  
- A_total (global asymmetry)  
- h (topological humility)  

RMK provides structure.
It does not evaluate it.

---

## 7. GLOBAL CONDITION

Coherence function:

C(Φ) = 1 - (1/N) * Σ |Δ|

Constraint:

- C is descriptive, not target  
- RMK does not maximize C  

---

## 8. ROLE IN VECTAETOS

RMK is:

- substrate of Φ  
- input layer for Vortex  
- compatible with Epistemic Cryptography  

RMK is NOT:

- Vortex  
- AJE  
- agent system  

---

## 9. EXECUTION CONSTRAINT

RMK must remain:

- deterministic  
- stateless (except current configuration)  
- non-recursive  
- non-adaptive  

Execution form:

input → RMK → relational structure → end

---

## 10. ANTI-CORRUPTION CLAUSE

If RMK:

- introduces optimization  
- introduces selection  
- introduces feedback  
- modifies Φ autonomously  

Then:

→ RMK is no longer RMK  
→ System becomes NON_VECTAETOS  

---

## 11. ONTOLOGICAL STATUS

RMK has:

- no claim to truth  
- no claim to authority  
- no claim to correctness  

It exists only as:

> a consistent relational configuration  

---

## 12. ROOT BINDING

RMK is bound to:

- ontology_hash.json  
- Φ invariants (so(8), triality)  

RMK cannot be separated from these without loss of identity.

---

## 13. FINAL STATEMENT

RMK does not produce knowledge.

RMK defines the minimal structure in which
knowledge may or may not emerge
without being enforced.

---

END
